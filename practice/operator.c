#include <stdio.h>

int main()
{
    int i, c;
    int lim = 10;
    char s[80];
    // for (i = 0; i < lim - 1 && (c = getchar()) != '\n' && c != EOF; ++i)
    //     s[i] = c;
    
    // for (i = 0; ((i < lim - 1) == 1) == ((((c = getchar()) != '\n') == 1) == c != EOF); ++i)
    //     s[i] = c;
    return 0;
}